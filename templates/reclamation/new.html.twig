{% extends 'base.html.twig' %}

{% block title %}New Reclamation{% endblock %}

{% block body %}
<link href="{{ asset('assets/css/reclamation/reclamation.css') }}" rel="stylesheet">
  <!-- ***** Header Area Start ***** -->
  <header class="header-area header-sticky">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <nav class="main-nav">
                    <!-- ***** Menu Start ***** -->
                    <ul class="nav">
                        <li><a href="{{ path('app_post_index') }}"  class="active">posts</a></li>
                        <li><a href="{{ path('app_reclamation_new') }}"  class="active">reclamation</a></li>
                    </ul>   
                    <!-- ***** Menu End ***** -->
                </nav>
            </div>
        </div>
    </div>
  </header>
  <!-- ***** Header Area End ***** -->
<div class="page-heading">
    <div class="container">
      <div class="row">
        <div class="col-lg-8 offset-lg-2 header-text">
          <h2><em>Contact US</em></h2>
          <p>If you are findinf some probelms try to contact us </p>
        </div>
      </div>
    </div>
  </div>
  {% for type, flashMessages in app.flashes %}
  {% for flashMessage in flashMessages %}
      <div class="alert alert-{{ type }}">
          {{ flashMessage }}
      </div>
  {% endfor %}
{% endfor %}
    {{ include('reclamation/_form.html.twig') }}

    <a href="{{ path('app_reclamation_index') }}">back to list</a>
{% endblock %}
