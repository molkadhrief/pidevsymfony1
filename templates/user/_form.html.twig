{{ form_start(form) }}
    {{ form_widget(form) }}
    <button  style="background-color: #4e73df;color:white;padding:8px 18px;border-radius: 10px; border: 2px solid #4e73df ;margin-top:12px;margin-bottom:15px;cursor: pointer;">
    {{ button_label|default('Save') }}
    </button>
    
{{ form_end(form) }}
