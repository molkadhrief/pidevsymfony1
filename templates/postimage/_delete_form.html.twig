<form method="post" action="{{ path('app_postimage_delete', {'id': postimage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postimage.id) }}">
    <button class="btn">Delete</button>
</form>
