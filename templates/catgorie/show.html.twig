{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ categorie.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_catgorie_index') }}">back to list</a>

    <a href="{{ path('app_catgorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('catgorie/_delete_form.html.twig') }}
{% endblock %}
